// Arquivo de seed migrado e protegido para evitar exposicao de senhas no bundle do cliente.
// A conta de treinador ja se encontra criada no Firebase Authentication.
export const runSeed = async () => {
  return true;
};
